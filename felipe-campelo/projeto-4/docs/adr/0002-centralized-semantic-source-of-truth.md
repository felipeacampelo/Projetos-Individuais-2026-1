# Keep semantic truth centralized outside transient prompts

The system's canonical semantic knowledge lives in explicit domain artifacts rather than being scattered across prompts, parser branches, and one-off extraction exceptions. We chose this so normalization, reprocessing, and auditability remain stable over time while prompts act as consumers of shared domain truth instead of becoming the hidden authority.
