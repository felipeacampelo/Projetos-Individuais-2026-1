# PDF Fixtures

This directory is reserved for two real fixture PDFs in later validation:

- one tabular quarterly result document
- one slide-style quarterly result document

The runtime environment for this thread does not currently include downloaded source PDFs, so the milestone leaves the fixture contract in place and uses HTML fixtures plus parsing/contract scaffolding for now.
